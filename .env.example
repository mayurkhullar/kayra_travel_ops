# Absolute or repo-relative path to your Firebase Admin service-account JSON
FIREBASE_SERVICE_ACCOUNT_PATH=./service-account.json

# Optional if present in service account, but useful when running against multiple projects
FIREBASE_PROJECT_ID=your-firebase-project-id
